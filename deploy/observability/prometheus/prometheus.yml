global:
  scrape_interval: 15s

scrape_configs:
  - job_name: fs-engine
    metrics_path: /metrics
    params:
      format: ["prometheus"]
    static_configs:
      - targets:
          - backend:8080
